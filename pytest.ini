[pytest]
testpaths = tests
addopts = -q
# The tidal-energy polynomial fits intentionally evaluate log10(0) -> -inf in the
# high-eta regime (the result is then zeroed); silence that expected numerical noise.
filterwarnings =
    ignore:divide by zero encountered in log10:RuntimeWarning
    ignore:invalid value encountered:RuntimeWarning
